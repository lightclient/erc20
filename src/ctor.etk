pc                              # [0]

# give deployer initial supply
push4 10000			# [balance, 0]
caller				# [caller, balance, 0]
sstore				# [0]

%push(end - start)		# [length, 0]
swap1				# [0, length]
dup2				# [length, 0, length]
push1 start			# [start, length, 0, length]
dup3				# [0, start, length, 0, length]
codecopy                        # [0, length]
return                          # []

start:
%include("jump_table.etk")
end:
